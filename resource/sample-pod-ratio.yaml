apiVersion: v1
kind: Pod
metadata:
  name: sample-pod-ratio
  namespace: dev
spec:
  containers:
  - name: nginx-container
    image: nginx
    resources:
      requests:
        cpu: 125m
      limits:
        cpu: 500m
